"""Nightly reflection + nudge cron — filled in at Step 11."""

from __future__ import annotations

from typing import Any, Optional


def run(*, adapter: Optional[Any] = None) -> dict:
    """Stub. Filled in at Step 11. Returns summary stats."""
    return {"batches": 0, "files": 0, "proposals": 0,
            "nudges_sent": 0, "actions_stale": 0}
