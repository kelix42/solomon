"""Proactive inbound flow — filled in at Step 10."""

from __future__ import annotations


def dispatch_pending_notifications(*, session=None) -> int:
    """Stub. Filled in at Step 10. Returns number of notifications sent."""
    return 0
