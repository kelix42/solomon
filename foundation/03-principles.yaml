# 03-principles.yaml — derived summary; canonical store is db/schemas/captured_items.sql.
# Topics: people rules / deal rules / speed rules / conflict rules / money rules / exit rules / hard lessons / core decision logic
# Filled by solomon-onboarding-03-principles or by intake.md -> solomon-extraction.
# Sleep-Cycle Job 12 (yaml-reconcile) catches drift; DB-wins per §1.

last_updated: null

# (placeholder — filled at end-of-onboarding from captured_items rows where domain matches this YAML)
raw_notes: |
