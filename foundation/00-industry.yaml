# 00-industry.yaml — derived summary; canonical store is db/schemas/captured_items.sql.
# Topics: industry / sector / geography / customers / ecosystem / risks / trends
# Filled by solomon-onboarding-00-industry or by intake.md -> solomon-extraction.
# Sleep-Cycle Job 12 (yaml-reconcile) catches drift; DB-wins per §1.

last_updated: null

# (placeholder — filled at end-of-onboarding from captured_items rows where domain matches this YAML)
raw_notes: |
