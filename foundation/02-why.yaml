# 02-why.yaml — derived summary; canonical store is db/schemas/captured_items.sql.
# Topics: origin / ultimate goal / freedom / who-for / finish line / failure / rejected success / fuel
# Filled by solomon-onboarding-02-why or by intake.md -> solomon-extraction.
# Sleep-Cycle Job 12 (yaml-reconcile) catches drift; DB-wins per §1.

last_updated: null

# (placeholder — filled at end-of-onboarding from captured_items rows where domain matches this YAML)
raw_notes: |
